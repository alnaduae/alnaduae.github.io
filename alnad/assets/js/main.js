const nav=document.querySelector('.navbar');
window.addEventListener('scroll',()=>{nav?.classList.toggle('scrolled',window.scrollY>20);const top=document.querySelector('.to-top');if(top)top.style.display=window.scrollY>500?'grid':'none'});
const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)e.target.classList.add('show')}),{threshold:.12});document.querySelectorAll('.reveal').forEach(el=>observer.observe(el));
const year=document.querySelector('[data-year]');if(year)year.textContent=new Date().getFullYear();
document.querySelectorAll('[data-count]').forEach(el=>{const target=+el.dataset.count;let done=false;const io=new IntersectionObserver(es=>{if(es[0].isIntersecting&&!done){done=true;let n=0;const step=Math.max(1,Math.ceil(target/40));const t=setInterval(()=>{n=Math.min(target,n+step);el.textContent=n+'+';if(n>=target)clearInterval(t)},30)}},{threshold:.6});io.observe(el)});
document.querySelectorAll('.filter-btn').forEach(btn=>btn.addEventListener('click',()=>{document.querySelectorAll('.filter-btn').forEach(b=>b.classList.remove('active'));btn.classList.add('active');const f=btn.dataset.filter;document.querySelectorAll('[data-category]').forEach(card=>card.closest('.product-item').style.display=f==='all'||card.dataset.category.includes(f)?'':'none')}));
document.querySelectorAll('[data-gallery]').forEach(img=>img.addEventListener('click',()=>{const modal=document.getElementById('galleryModal');if(modal){modal.querySelector('img').src=img.src;new bootstrap.Modal(modal).show()}}));
const form=document.querySelector('#quoteForm');form?.addEventListener('submit',e=>{e.preventDefault();const name=form.querySelector('[name=name]')?.value||'there';const toast=document.getElementById('quoteToast');if(toast){toast.querySelector('.toast-body').textContent=`Thank you, ${name}. Your enquiry is ready to be reviewed by ALNAD UAE.`;new bootstrap.Toast(toast).show()}form.reset()});
